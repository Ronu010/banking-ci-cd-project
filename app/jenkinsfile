ppipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Pipeline is running!'
            }
        }
        stage('Build') {
            steps {
                bat 'docker build -t banking-app .'
            }
        }
    }
}
