pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Pipeline is running!'
            }
        }
        stage('Build') {
            steps {
                echo 'Starting Docker build...'
                bat 'docker build -t banking-app -f app/Dockerfile app'
            }
        }
    }
}

